# Application
APP_ENV=development
APP_PORT=8080

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=userdb
POSTGRES_PORT=5432

# Full database connection string (used by the Go app)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/userdb?sslmode=disable
